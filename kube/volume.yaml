apiVersion: v1
kind: PersistentVolume
metadata:
  name: db-sql-pv #mysql-pv-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/Users/sergey_chuvakin/Desktop/DVC_CML_sandbox/data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-sql-pvc # mysql-pv-data
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi